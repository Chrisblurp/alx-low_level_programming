#include "main.h"
/**
* _isdigit - evalue c is a digit
* @c: checks for the value of c
* Return: 0 Always.
*/
int _isdigit(int c)
{

if (c >= '48' && c <= '57')
{
return (1);
}
else
{
return (0);
}

}


