#include <stdio.h>
/**
  * Main - Entry Point
  *
  * Return: Always 0 (Success/correct)
 */
int main(void)
{
char alpha;
for (alpha = 'z'; alpha <= 'a'; alpha++)
{
putchar(alpha);
}
putchar('\n');
return (0);
}
