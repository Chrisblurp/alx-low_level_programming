#include <stdio.h>
/**
  * Main - Entry point
  *
  * Return: Always 0 (Success/correct)
  */
int main(void)
{
char alpha;
for (alpha = 'a'; alpha <= 'z'; alpha++)
{
putchar(alpha);
}
putchar('\n');
return (0);
}
